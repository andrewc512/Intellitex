/**
 * LaTeX compile (placeholder).
 * Main process will spawn latexmk/pdflatex and parse log for errors.
 */

// const { spawn } = require('child_process');
// function compile(mainPath, cwd) { ... }
// function parseLogErrors(logPath) { ... }
// module.exports = { compile, parseLogErrors };
